# Repository history

## Status

The original Git commit history for this project is **no longer available** (lost due to a local/backup incident). This repository therefore does **not** contain the prior sequence of commits, authors, dates, or per-change diffs from before that loss.

## What this tree represents

The current branch is the **baseline codebase** agreed as the source of truth after recovery: application source, configuration templates, and tooling as present at the time this note was added.

## For the team

- Treat this snapshot as the **starting point** for all new work going forward.
- Prefer **small, focused commits** with clear messages so history remains useful.
- Keep a **remote on GitHub (or similar)** as the canonical backup; avoid a single-machine-only copy.
- If anyone still has an **old clone, fork, or zip** from before the loss, compare it with this tree and open a PR if anything differs.

## Tags / releases

If you previously used version tags that are now missing, recreate them only when they match **verified** builds (e.g. deployment records), not by guessing dates.

---

*This file exists to document the situation transparently for reviewers and new contributors.*
